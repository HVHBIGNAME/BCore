[package]
name = "bcore-plugin"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "BCore native plugin API and manager"

[dependencies]
bcore-core = { path = "../bcore-core" }
libloading = "0.8"

[[example]]
name = "native_plugin"
crate-type = ["cdylib"]
