[package]
name = "bcore-registry"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "BCore data-driven block/item registry (registry sync)"

[dependencies]
bcore-core = { path = "../bcore-core" }
