[package]
name = "bcore-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "BCore Minecraft Java Edition protocol (26.2 / protocol 776)"

[dependencies]
bcore-core = { path = "../bcore-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
